{
  "name": "auth",
  "version": "1.0.0",
  "description": "Server to connect the frontend to the database, authentification of users and set preferences",
  "main": "entry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "thib",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-promise-router": "^3.0.3",
    "pg": "^8.0.3"
  }
}
